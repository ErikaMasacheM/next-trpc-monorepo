{
  "name": "next-trpc-monorepo",
  "private": true,
  "license": "MIT",
  "workspaces": ["apps/*", "packages/*"],
  "packageManager": "yarn@1.22.21",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.5.5"
  }
}
